import 'package:flutter/material.dart';

class AmeraApps extends StatelessWidget {
  const AmeraApps({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('amer apps'),
      ),
      backgroundColor: Colors.red,
    );
  }
}
